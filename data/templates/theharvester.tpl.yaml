apikeys:

  bevigil:
    key: "{{ bevigil.key }}"

  bitbucket:
    key: "{{ bitbucket.key }}"

  brave:
    key: "{{ brave.key }}"

  # Beware of the typo here, I consider theHarvester made a typo and I prefer bufferoverrun in input file
  bufferoverun:
    key: "{{ bufferoverrun.key }}"

  builtwith:
    key: "{{ builtwith.key }}"

  censys:
    id: "{{ censys.id }}"
    secret: "{{ censys.secret }}"

  criminalip:
    key: "{{ criminalip.key }}"

  dehashed:
    key: "{{ dehashed.key }}"

  dnsdumpster:
    key: "{{ dnsdumpster.key }}"

  fofa:
    key: "{{ fofa.key }}"
    email: "{{ fofa.email }}"

  fullhunt:
    key: "{{ fullhunt.key }}"

  github:
    key: "{{ github.key }}"

  hackertarget:
    key: "{{ hackertarget.key }}"

  haveibeenpwned:
    key: "{{ haveibeenpwned.key }}"

  hunter:
    key: "{{ hunter.key }}"

  hunterhow:
    key: "{{ hunterhow.key }}"

  intelx:
    key: "{{ intelx.key }}"

  leakix:
    key: "{{ leakix.key }}"

  leaklookup:
    key: "{{ leaklookup.key }}"

  mojeek:
    key: "{{ mojeek.key }}"
  
  netlas:
    key: "{{ netlas.key }}"

  onyphe:
    key: "{{ onyphe.key }}"

  pentestTools:
    key: "{{ pentestTools.key }}"

  projectDiscovery:
    key: "{{ projectDiscovery.key }}"

  rocketreach:
    key: "{{ rocketreach.key }}"

  securityscorecard:
    key: "{{ securityscorecard.key }}"

  securityTrails:
    key: "{{ securityTrails.key }}"

  shodan:
    key: "{{ shodan.key }}"

  tomba:
    key: "{{ tomba.key }}"
    secret: "{{ tomba.secret }}"

  venacus:
    key: "{{ venacus.key }}"

  virustotal:
    key: "{{ virustotal.key }}"

  whoisxml:
    key: "{{ whoisxml.key }}"

  windvane:
    key: "{{ windvane.key }}"

  zoomeye:
    key: "{{ zoomeye.key }}"