modules:
  shodan_dns:
    api_key: "{{ shodan.key }}"

  securitytrails:
    api_key: "{{ securitytrails.key }}"

  fullhunt:
    api_key: "{{ fullhunt.key }}"

  github_codesearch:
    api_key: "{{ github.key }}"

  hunterio:
    api_key: "{{ hunter.key }}"

  binaryedge:
    api_key: "{{ binaryedge.key }}"

  censys:
    api_id: "{{ censys.id }}"
    api_secret: "{{ censys.secret }}"