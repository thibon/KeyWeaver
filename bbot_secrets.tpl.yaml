modules:
  shodan_dns:
    api_key: "{{ shodan }}"

  securitytrails:
    api_key: "{{ securitytrails }}"

  fullhunt:
    api_key: "{{ fullhunt }}"

  github_codesearch:
    api_key: "{{ github }}"

  hunterio:
    api_key: "{{ hunter }}"

  binaryedge:
    api_key: "{{ binaryedge }}"

  censys:
    api_id: "{{ censys_id }}"
    api_secret: "{{ censys_secret }}"